{
  "name": "github-repo-list",
  "version": "1.0.0",
  "description": "list the repos and there clone url for a given github.com username",
  "main": "index.js",
  "dependencies": {
    "chalk": "^1.1.1",
    "request": "^2.67.0"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "james connelly",
  "bin": {
    "github-repo-list": "index.js"
  }
}
